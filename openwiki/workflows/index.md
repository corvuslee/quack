# Files

- [Fetch and JavaScript Render Workflows](content-retrieval.md) - How Quack acquires a page through the lightweight HTTP fetch path or the optional SeleniumBase JavaScript-rendering path, then produces the same Markdown contract. Covers URL boundaries, session and dependency behavior, CLI sinks, failures, and focused verification.
- [DuckDuckGo Search Pipeline](search-pipeline.md) - Traces a Quack search from Python or CLI input through impersonated DuckDuckGo HTML retrieval, parsing and normalization, retry and challenge behavior, to ordered result records or typed failures.
